# OnOlam — Environment variables
# Bu faylni nusxa oling: cp .env.example .env
# Keyin .env faylini to'ldiring

# ── DJANGO ──
SECRET_KEY=your-very-secret-key-here-change-this
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,onolam.uz

# ── DATABASE (PostgreSQL) ──
DB_ENGINE=django.db.backends.postgresql
DB_NAME=onolam_db
DB_USER=onolam_user
DB_PASSWORD=your_db_password
DB_HOST=localhost
DB_PORT=5432

# ── DATABASE (SQLite — development uchun) ──
# DB_ENGINE=django.db.backends.sqlite3
# DB_NAME=db.sqlite3

# ── JWT TOKENS ──
JWT_ACCESS_TOKEN_LIFETIME_MINUTES=60
JWT_REFRESH_TOKEN_LIFETIME_DAYS=30

# ── CORS ──
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080,https://onolam.uz

# ── MEDIA ──
MEDIA_URL=/media/
MEDIA_ROOT=media/

# ── PAYME (To'lov tizimi) ──
PAYME_ID=your_payme_merchant_id
PAYME_KEY=your_payme_key
PAYME_TEST_KEY=your_payme_test_key
PAYME_URL=https://checkout.paycom.uz

# ── CLICK ──
CLICK_MERCHANT_ID=your_click_merchant_id
CLICK_SERVICE_ID=your_click_service_id
CLICK_SECRET_KEY=your_click_secret

# ── EMAIL ──
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_HOST_USER=hello@onolam.uz
EMAIL_HOST_PASSWORD=your_email_password
EMAIL_USE_TLS=True

# ── REDIS ──
REDIS_URL=redis://localhost:6379/0

# ── PRO TARIF ──
PRO_MONTHLY_PRICE=9.00
PRO_YEARLY_PRICE=75.00
